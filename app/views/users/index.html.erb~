<p id="notice"><%= notice %></p>



  <% if !(current_user) %>
    <h1> Login Needed </h1>
    <%= link_to 'Login', '/login' %> | <%= link_to 'Signup', '/signup' %>
  <% else %>
    <% if current_user.isadmin == 0 %>
      <script type="text/javascript">
        window.location.href="notadmin"
      </script>
    <% else %>
  

<h1>Listing Users</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Password</th>
      <th>Gender</th>
      <th>Isadmin</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.password %></td>
        <td><%= user.gender %></td>
        <td><%= user.isadmin %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>





<br > <br> <br>




<% if current_user %>
  Signed in as <%= link_to 'Edit', edit_user_path(current_user.id) %> | <%= link_to "Logout", '/logout' %>
  <a href="<%= edit_user_path(current_user.id) %>"><%= current_user.name %></a>
<% else %>
  <%= link_to 'Login', '/login' %> | <%= link_to 'Signup', '/signup' %>
<% end %>

<%= yield %>




  <% end %>
  <% end %>
